apiVersion: v2
name: helm-minio
version: 1.0.0
appVersion: 1.23.0
description: A Helm chart wrapper for MinIO

dependencies:
  - name: minio
    version: "5.4.0"
    repository: "https://charts.min.io/"
    condition: minio.enabled
