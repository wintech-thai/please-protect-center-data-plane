apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: geoip-distributor
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'geoip-distributor-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/please-protect-center-data-plane.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/geoip-distributor'
      destination:
        name: in-cluster
        namespace: geoip-distributor
      syncPolicy:
        automated:
          selfHeal: false
          prune: true
        syncOptions:
          - CreateNamespace=true
