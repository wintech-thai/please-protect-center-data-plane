apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: opencti
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'opencti-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/please-protect-center-data-plane.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/opencti'
      destination:
        name: in-cluster
        namespace: opencti
      syncPolicy:
        automated:
          selfHeal: false
          prune: true
        syncOptions:
          - CreateNamespace=true
